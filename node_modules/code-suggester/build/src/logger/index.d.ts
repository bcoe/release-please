import { Logger, Level } from 'pino';
import * as Pino from 'pino';
declare let logger: Logger;
declare function setupLogger(userLogger?: Logger | Pino.LoggerOptions): void;
export { logger, Logger, Level, setupLogger };
